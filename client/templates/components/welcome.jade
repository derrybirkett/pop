template(name="welcome")
  .usercard.cover(style="background-image:url('#{services.twitter.profile_image_url}');")
    .site-wrapper
      .site-wrapper-inner
        .cover-container
          .inner.cover
            .usercard.basic
              h1 Welcome
              h5 Why not login ?
              +loginButtons
