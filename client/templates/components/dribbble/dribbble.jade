template(name="dribbble")
  if account.dribbble.username
    +dribbbleShots
  else
    if currentUser
      .dribbble
        form
          input(type="text", name="username", placeholder="Add your Dribbble username").form-control.input-lg
