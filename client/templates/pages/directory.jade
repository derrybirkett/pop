template(name='directory')
  .usercard.cover(style="background-image:url('#{services.twitter.profile_image_url}');")
    .site-wrapper
      .site-wrapper-inner
        .cover-container
          .inner.cover
            section.directory
              h1 Directory
              p Here are the registered users
              each user
                +usercard
                
